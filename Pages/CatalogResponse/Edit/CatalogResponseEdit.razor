@* @page "/respuestasPredeterminadas/editar" *@
@inherits CatalogResponseEditBase
<div>

    <EditForm Model="managementCatalogResponseEdit" FormName="EditForm" OnValidSubmit="HandleFormValid">
        <DataAnnotationsValidator />
        <div class="row">
            <div class="mt-3 col-12">
                <div class="form-group marginCustom">
                    <label for="tipoCedula">Tipo de estado *</label>
                    <Select Items="itemsCatalogSelect" id="tipoCedula" @bind-Value="managementCatalogResponseEdit.Code"></Select>
                    <ValidationMessage For="@(() => managementCatalogResponseEdit.Code)" />
                </div>
            </div>
            <div class="mt-3 col-12">
                <div class="form-group marginCustom">
                    <label>Título respuesta *</label>
                    <InputText class="form-control" @bind-Value="managementCatalogResponseEdit.DisplayLabel"></InputText>
                    <ValidationMessage For="@(() => managementCatalogResponseEdit.DisplayLabel)" />
                </div>
            </div>
            @*        <div class="mt-3 col-md-6 col-sm-12">
            <div class="form-group">
            <label>Detalle respuesta</label>
            <InputText class="form-control" @bind-Value="managementCatalogResponseEdit.Description"></InputText>

            <Textarea class="form-control"  rows="4" @bind-Value="managementCatalogResponseEdit.Description" />

            <ValidationMessage For="@(() => managementCatalogResponseEdit.Description)" />
            </div>
            </div> *@

            <div class="mt-3 col-12">
                <div class="form-group">
                    <label>Detalle respuesta *</label>
                    @* <InputText class="form-control" @bind-Value="managementCatalogResponseEdit.Description"></InputText> *@
                    <Textarea class="form-control" rows="4" @bind-Value="managementCatalogResponseEdit.Description" />
                    <ValidationMessage For="@(() => managementCatalogResponseEdit.Description)" />
                </div>
            </div>

        </div>
        <div class="row custom-buttons">
            <div class="d-flex-center col-md-6 col-sm-12">
                <AuthorizeScopeDirective Scope="Edit:CatalogResponse">
                    <div class="input-submit-icon button-general-form">
                        <input type="submit" value="Editar">
                    </div>
                </AuthorizeScopeDirective>
            </div>

            <div class="d-flex-center col-md-6 col-sm-12">
                <button type="button" class="custom-button-close button-general-form" @onclick="CloseModal">
                    @* <i class="bi bi-x"></i> *@
                    Cancelar
                </button>
            </div>
        </div>
    </EditForm>



</div>